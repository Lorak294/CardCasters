import type { PageLoad } from './$types';

export const load: PageLoad = async ({ params, fetch }) => {
	// load data from api here
	//const res = await fetch(`/api/deck?id=${params.deckid}`);
	//const deck = (await res.json()) as Deck;
	//return { deck };
};
